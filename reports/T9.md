# T9 — Promote key rulebook surfaces out of hidden menu

## Status: ✅ DONE

## Objective

Replace the hidden "More" dropdown with visible inline tab navigation so all rulebook sections are discoverable without extra clicks.

## Preflight

- Baseline: clean working tree after T8 commit `45df3df`
- Build: `npm run build` exit 0, 63 routes

## Scope Confirmation

- **Target file:** `src/app/(app)/rulebooks/[id]/layout.tsx`
- **Before:** Schema, Tests, API hidden behind a "More" dropdown button
- **After:** 5 visible tabs: Overview | Rules | Schema | Tests | API

## Execution Packet

### Changes Made

| Area | Change |
|------|--------|
| Imports | Removed `MoreHorizontal`, DropdownMenu components; added `LayoutDashboard`, `List` icons |
| Header | Removed DropdownMenu JSX block |
| Tab navigation | Added `<nav>` with 5 tab links (Overview, Rules, Schema, Tests, API) |
| Active state | Brand-colored bottom border + text for active tab |
| Mobile | `overflow-x-auto` with hidden scrollbar for horizontal scrolling |
| Accessibility | `role="tablist"` on nav, `role="tab"` + `aria-selected` on each link |

### Acceptance Criteria

| Criterion | Evidence |
|-----------|----------|
| Core tabs visible without opening dropdown | All 5 tabs rendered inline in header |
| Mobile has accessible alternate nav pattern | `overflow-x-auto` enables horizontal scroll on small screens |

### Validation Gates

| Gate | Result |
|------|--------|
| `npm run build` (post-change) | **exit 0**, 63 routes ✅ |
| Checklist updated | `T9` → `[x]` ✅ |

## Artifacts

- Modified: `src/app/(app)/rulebooks/[id]/layout.tsx`
- Updated: `MVP-CHECKLIST.md` (T9 `[x]`)
- Report: `reports/T9.md` (this file)
