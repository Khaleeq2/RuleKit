# T1 — Fix misleading file claims on marketing

## Status: ✅ DONE

## Objective

Replace marketing claims that imply "any file type" is supported with truthful, implemented supported-format language.

## Preflight

### 1) Repository state

- Baseline commit: `fdba1e1` (`chore: baseline commit before MVP T1`)
- Working tree after baseline: clean (only `reports/` untracked)

### 2) Scope confirmation

- **Checklist-specified files:**
  - `src/app/(marketing)/_components/landing/HeroSectionV2.tsx` — 2 hits ("any file" on lines 43, 137)
  - `src/app/(marketing)/_lib/public-content.ts` — 0 hits (no misleading claims found)
- **Additional scan:** `grep -r "any file" src/` — confirmed only 2 hits, both in `HeroSectionV2.tsx`
- **Actual supported formats** (from `src/app/(app)/home/page.tsx`): JSON, CSV, TXT, XML, YAML, YML, TSV, LOG, MD, HTML — all text-based

### 3) Build baseline

- `npm run build` — exit code 0, 63 routes, clean compile

### 4) Lint baseline

- `npm run lint` script does not exist in `package.json` — skipped per plan

## Execution Packet

### Assumptions + Evidence

1. The app only supports text-based file formats (verified via `processFile()` in `home/page.tsx` lines 1024-1035 and `accept` attribute on line 1189)
2. `public-content.ts` had no "any file" claims (grep confirmed 0 hits)
3. No other files in `src/` contain "any file" claims (grep confirmed)

### Change Plan

Two copy edits in `HeroSectionV2.tsx`:

| Location | Before | After |
|----------|--------|-------|
| Line 43 (hero subhead) | `Upload any file or content and get instant, AI‑powered verdicts.` | `Paste text or upload a file (JSON, CSV, TXT, XML, YAML, Markdown) and get instant, AI‑powered verdicts.` |
| Line 137 (bottom feature) | `Same standards across docs, forms, AI, and any file type.` | `Same standards across docs, forms, AI outputs, and structured data.` |

### Implementation

- File modified: `src/app/(marketing)/_components/landing/HeroSectionV2.tsx`
- 2 string replacements, 0 logic changes

### Validation Gates

| Gate | Result |
|------|--------|
| `grep -r "any file" src/` | **0 hits** ✅ |
| `npm run build` (post-change) | **exit 0**, 63 routes ✅ |
| Checklist updated | `T1` → `[x]`, `C1` → `[x]` ✅ |

## Artifacts

- Modified: `src/app/(marketing)/_components/landing/HeroSectionV2.tsx`
- Updated: `MVP-CHECKLIST.md` (T1 `[x]`, C1 `[x]`)
- Report: `reports/T1.md` (this file)
