# T5 — Match keyboard submit hint to real behavior

## Status: ✅ DONE

## Objective

Fix the mismatch between the submit button's keyboard hint (⌘+Enter) and the actual behavior (Enter submits, Shift+Enter for newline).

## Preflight

- Baseline: clean working tree after T4 commit `6b20b04`
- Build: `npm run build` exit 0, 63 routes

## Scope Confirmation

- **Target file:** `src/app/(app)/home/page.tsx`
- **Mismatch found:**
  - Tooltip: `"Submit (⌘+Enter)"` — implies Cmd+Enter required
  - Actual handler (line 622): `if (e.key === 'Enter' && !e.shiftKey)` — plain Enter submits
- **Broader scan:** No other `⌘+Enter` keyboard hints found in `src/`

## Execution Packet

### Decision

Update tooltip to match existing behavior. Plain Enter to submit is the standard pattern (ChatGPT, Slack, etc.). No behavior change needed.

### Change

| Location | Before | After |
|----------|--------|-------|
| Submit button `title` | `"Submit (⌘+Enter)"` | `"Submit (Enter)"` |

### Validation Gates

| Gate | Result |
|------|--------|
| Tooltip matches actual keyboard handler | ✅ |
| No other misleading keyboard hints in codebase | ✅ |
| `npm run build` (post-change) | **exit 0**, 63 routes ✅ |
| Checklist updated | `T5` → `[x]`, `C11` → `[x]` ✅ |

## Artifacts

- Modified: `src/app/(app)/home/page.tsx`
- Updated: `MVP-CHECKLIST.md` (T5 `[x]`, C11 `[x]`)
- Report: `reports/T5.md` (this file)
